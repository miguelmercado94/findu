/**
 * Puertos de salida — interfaces que la capa de aplicación define para que la infraestructura implemente
 * (repositorios, servicios externos, etc.)
 */
package com.findu.core.application.port.output.persistence;
