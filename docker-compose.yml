# FINDU — stack local (Docker Compose)
# =====================================================================
# Orden de arranque:
#   1. Red privada: findu-internal (solo acceso inter-contenedores)
#   2. Arquitectura: eureka-server → api-gateway (expuestos al host)
#   3. Bases de datos: postgres-security, postgres-oauth2 (red privada)
#   4. Seguridad: findu-spring-security, autorization-server-oauth2 (red privada, perfil dev)
#
# Frontends (Vite): se levantan localmente (no Docker)
#   - cliente:   npm run dev → http://localhost:3000
#   - proveedor: npm run dev → http://localhost:3001
#
# Uso:
#   docker compose up -d
#   Luego en terminales separadas:
#     cd FRONTEND/cliente   && npm run dev
#     cd FRONTEND/proveedor && npm run dev
# =====================================================================

services:

  # ─────────────────────────────────────────────────────────────────────
  # ARQUITECTURA (expuestos al host)
  # ─────────────────────────────────────────────────────────────────────

  eureka-server:
    image: mmercado94/findu-eureka-server:1.0.0
    container_name: findu-eureka-server
    networks:
      - findu-internal
    ports:
      - "8761:8761"
    environment:
      # --- Eureka Server: config optimizada para dev local ---
      # Eviction check cada 5s (default 60s) — detecta servicios caídos rápido
      EUREKA_SERVER_EVICTION_INTERVAL_TIMER_IN_MS: "5000"
      # Response cache refresh cada 5s (default 30s) — los clientes ven cambios rápido
      EUREKA_SERVER_RESPONSE_CACHE_UPDATE_INTERVAL_MS: "5000"
      # Deshabilitar self-preservation: en dev con pocos servicios, evictar inmediatamente
      # EN PRODUCCIÓN: SIEMPRE debe ser true
      EUREKA_SERVER_ENABLE_SELF_PRESERVATION: "false"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8761/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  api-gateway:
    image: mmercado94/findu-api-gateway:1.0.0
    container_name: findu-api-gateway
    depends_on:
      eureka-server:
        condition: service_healthy
    networks:
      - findu-internal
    ports:
      - "8080:8080"
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      # Registry fetch cada 5s (default 30s) — gateway descubre servicios rápido
      EUREKA_CLIENT_REGISTRY_FETCH_INTERVAL_SECONDS: "5"
      # LoadBalancer cache TTL reducido (default 35s) — evita routing a instancias stale
      SPRING_CLOUD_LOADBALANCER_CACHE_TTL: "5s"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ─────────────────────────────────────────────────────────────────────
  # BASES DE DATOS (red privada, sin puertos expuestos al host)
  # ─────────────────────────────────────────────────────────────────────

  postgres-security:
    image: postgres:16-alpine
    container_name: findu-postgres-security
    networks:
      - findu-internal
    environment:
      POSTGRES_USER: findu
      POSTGRES_PASSWORD: findu
      POSTGRES_DB: findu
    volumes:
      - findu-pg-security-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U findu -d findu"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  postgres-oauth2:
    image: postgres:16-alpine
    container_name: findu-postgres-oauth2
    networks:
      - findu-internal
    environment:
      POSTGRES_USER: findu_oauth2
      POSTGRES_PASSWORD: findu_oauth2_password
      POSTGRES_DB: findu_oauth2
    volumes:
      - findu-pg-oauth2-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U findu_oauth2 -d findu_oauth2"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ─────────────────────────────────────────────────────────────────────
  # SEGURIDAD (red privada, perfil dev, sin puertos expuestos al host)
  # ─────────────────────────────────────────────────────────────────────

  findu-spring-security:
    image: mmercado94/findu-spring-security:1.0.0
    container_name: findu-spring-security
    # hostname fija para que Java detecte correctamente el nombre de host
    # y Eureka lo registre con este valor (resuelto via DNS Docker)
    hostname: findu-spring-security
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres-security:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    networks:
      - findu-internal
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres-security:5432/findu
      SPRING_R2DBC_USERNAME: findu
      SPRING_R2DBC_PASSWORD: findu
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      # --- EUREKA INSTANCE: hostname-based registration (CLAVE para Docker) ---
      # prefer-ip-address=false → Eureka registra el hostname, no la IP
      # hostname=findu-spring-security → coincide con el DNS de Docker Compose
      # El gateway resuelve este hostname via DNS interno de Docker → IP correcta siempre
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "false"
      EUREKA_INSTANCE_HOSTNAME: findu-spring-security
      # Heartbeat cada 5s (default 30s) — Eureka sabe rápido que estamos vivos
      EUREKA_INSTANCE_LEASE_RENEWAL_INTERVAL_IN_SECONDS: "5"
      # Si no hay heartbeat en 15s (default 90s), Eureka nos da de baja
      EUREKA_INSTANCE_LEASE_EXPIRATION_DURATION_IN_SECONDS: "15"
      # Fetch del registry cada 5s
      EUREKA_CLIENT_REGISTRY_FETCH_INTERVAL_SECONDS: "5"
      # --- JWT ---
      JWT_SECRET: ZmluZHUtZGVmYXVsdC1zZWNyZXQta2V5LWF0LWxlYXN0LTI1Ni1iaXRzLWZvci1oczI1Ng==
      JWT_ACCESS_EXPIRATION_SECONDS: "3600"
      JWT_REFRESH_EXPIRATION_SECONDS: "604800"
      # --- Issuer URI (red interna) ---
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://autorization-server-oauth2:9595/authorization-server
      # --- Features habilitados para dev con Docker ---
      FINDU_REDIS_ENABLED: "true"
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      FINDU_AWS_DYNAMODB_ENABLED: "true"
      FINDU_AWS_DYNAMODB_ENDPOINT: http://localstack:4566
      FINDU_AWS_DYNAMODB_REGION: us-east-1
      FINDU_AWS_DYNAMODB_CREATE_TABLE_IF_NOT_EXISTS: "true"
      MANAGEMENT_HEALTH_REDIS_ENABLED: "true"
      # --- Notificaciones ---
      FINDU_NOTIFICATION_ENABLED: "true"
      FINDU_NOTIFICATION_LAMBDA_URL: http://notification-dispatcher:9000/dispatchNotification
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8081/security-auth/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s

  autorization-server-oauth2:
    image: mmercado94/findu-autorization-server-oauth2:1.0.0
    container_name: findu-autorization-server-oauth2
    hostname: findu-autorization-server-oauth2
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres-oauth2:
        condition: service_healthy
      findu-spring-security:
        condition: service_healthy
    networks:
      - findu-internal
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-oauth2:5432/findu_oauth2
      SPRING_DATASOURCE_USERNAME: findu_oauth2
      SPRING_DATASOURCE_PASSWORD: findu_oauth2_password
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      # --- EUREKA INSTANCE: hostname-based (mismo patrón) ---
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "false"
      EUREKA_INSTANCE_HOSTNAME: findu-autorization-server-oauth2
      EUREKA_INSTANCE_LEASE_RENEWAL_INTERVAL_IN_SECONDS: "5"
      EUREKA_INSTANCE_LEASE_EXPIRATION_DURATION_IN_SECONDS: "15"
      EUREKA_CLIENT_REGISTRY_FETCH_INTERVAL_SECONDS: "5"
      # --- Comunicación interna ---
      FINDU_SECURITY_API_BASE_URL: http://findu-spring-security:8081/security-auth
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9595/authorization-server/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s

  # ─────────────────────────────────────────────────────────────────────
  # CORE (red privada)
  # ─────────────────────────────────────────────────────────────────────

  postgres-core:
    image: postgres:16-alpine
    container_name: findu-postgres-core
    networks:
      - findu-internal
    environment:
      POSTGRES_USER: findu_core
      POSTGRES_PASSWORD: findu_core
      POSTGRES_DB: findu_core
    volumes:
      - findu-pg-core-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U findu_core -d findu_core"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  findu-core:
    build:
      context: ./CORE/findu-core
      dockerfile: Dockerfile
    container_name: findu-core
    hostname: findu-core
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres-core:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - findu-internal
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-core:5432/findu_core
      SPRING_DATASOURCE_USERNAME: findu_core
      SPRING_DATASOURCE_PASSWORD: findu_core
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "false"
      EUREKA_INSTANCE_HOSTNAME: findu-core
      EUREKA_INSTANCE_LEASE_RENEWAL_INTERVAL_IN_SECONDS: "5"
      EUREKA_INSTANCE_LEASE_EXPIRATION_DURATION_IN_SECONDS: "15"
      EUREKA_CLIENT_REGISTRY_FETCH_INTERVAL_SECONDS: "5"
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      FINDU_CACHE_ENABLED: "true"
      FINDU_NOTIFICATION_ENABLED: "true"
      FINDU_NOTIFICATION_LAMBDA_URL: http://notification-dispatcher:9000/dispatchNotification
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8082/findu-core/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s

  findu-notification-processor:
    build:
      context: ./CORE/findu-notification-processor
      dockerfile: Dockerfile
    container_name: findu-notification-processor
    hostname: findu-notification-processor
    depends_on:
      eureka-server:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    networks:
      - findu-internal
    environment:
      SPRING_PROFILES_ACTIVE: dev
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USERNAME: findu
      RABBITMQ_PASSWORD: findu
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "false"
      EUREKA_INSTANCE_HOSTNAME: findu-notification-processor
      DYNAMODB_ENDPOINT: http://localstack:4566
      SES_ENDPOINT: http://localstack:4566
      SNS_ENDPOINT: http://localstack:4566
      AWS_REGION: us-east-1
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8083/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s

  # ─────────────────────────────────────────────────────────────────────
  # MESSAGING & AWS (red privada)
  # ─────────────────────────────────────────────────────────────────────

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: findu-rabbitmq
    networks:
      - findu-internal
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: findu
      RABBITMQ_DEFAULT_PASS: findu
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  redis:
    image: redis:7-alpine
    container_name: findu-redis
    networks:
      - findu-internal
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  localstack:
    image: localstack/localstack:3.8
    container_name: findu-localstack
    networks:
      - findu-internal
    ports:
      - "4566:4566"
    environment:
      SERVICES: dynamodb,ses,sns,s3
      AWS_DEFAULT_REGION: us-east-1
      DEBUG: 0
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4566/_localstack/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  notification-dispatcher:
    build:
      context: ./CORE/LAMBDA/findu-notification-dispatcher
      dockerfile: Dockerfile
    container_name: findu-notification-dispatcher
    networks:
      - findu-internal
    ports:
      - "9000:9000"
    depends_on:
      rabbitmq:
        condition: service_healthy
      localstack:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: dev
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USERNAME: findu
      RABBITMQ_PASSWORD: findu
      DYNAMODB_ENDPOINT: http://localstack:4566
      AWS_REGION: us-east-1
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 45s

# ─────────────────────────────────────────────────────────────────────
# REDES Y VOLÚMENES
# ─────────────────────────────────────────────────────────────────────

networks:
  findu-internal:
    driver: bridge

volumes:
  findu-pg-security-data:
  findu-pg-oauth2-data:
  findu-pg-core-data:
