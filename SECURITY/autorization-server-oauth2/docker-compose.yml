version: '3.8'

services:
  oauth2-postgres:
    image: postgres:16-alpine
    container_name: oauth2-postgres
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: findu_oauth2
      POSTGRES_PASSWORD: findu_oauth2_password
      POSTGRES_DB: findu_oauth2
    volumes:
      - oauth2-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U findu_oauth2 -d findu_oauth2"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  oauth2-postgres-data:
