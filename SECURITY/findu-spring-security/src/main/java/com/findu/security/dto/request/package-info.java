/**
 * DTOs de entrada: modelos de datos para las peticiones (request body, query, etc.).
 * Las clases de este paquete suelen usarse en los controladores y se mapean al dominio o a puertos de entrada.
 */
package com.findu.security.dto.request;
